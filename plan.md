ここまでの議論を踏まえると、Claude Code CLI に渡すべきプロンプトは単なる「成果物を作れ」では不十分です。

今回の狙いは、

> 要求仕様書から機械的にテスト目的を生成し、機材変更・機能追加・回帰試験に対して、影響度に基づくテスト計画を策定できるテストアーキテクチャを構築すること

です。

つまり Claude に作業を依頼する際も、

* なぜこの成果物を作るのか
* 後続工程で何に使うのか
* どのような判断を人間がレビューするのか

まで伝えた方が精度が上がります。

私なら以下の「プロジェクト憲章」を各 Phase の前提として与えます。

# テストアーキテクチャ構築プロジェクト

## 背景

本プロジェクトはアミューズメントパーク向けシステムを対象としたシステムテストアーキテクチャ構築を目的とする。

対象システムは以下のような複数機材から構成される。

* Web購入システム
* 携帯アプリ購入システム
* 金融決済サーバ
* 改札機
* 発券機
* 入場者数管理サーバ
* 全体管理サーバ

システムは継続的に機能追加および環境変更が発生する。

従来は担当者の経験に依存してテスト範囲を決定していたため、

* 評価範囲の属人化
* 回帰範囲の肥大化
* 現地試験の長期化
* 手戻り増加

が発生している。

---

## 目的

要求仕様書およびシステム構成情報から以下を機械的に導出可能なテストアーキテクチャを構築する。

### 要求分析

要求文を分析し構造化する。

### 機能モデル生成

要求を機能単位へ分類する。

### 機材モデル生成

機材依存関係をグラフ化する。

### 品質モデル生成

品質特性との関係を整理する。

### テスト目的生成

要求群から評価すべきテスト目的を生成する。

### 影響分析

機材変更や機能追加時の影響範囲を定量化する。

### テスト計画生成

テスト目的を優先順位付けし評価順序を決定する。

---

## 最終的に実現したいこと

変更要求が入力された際、

「何を評価すべきか」

「どこまで評価すべきか」

「ベンチで実施可能か」

「現地試験が必要か」

を定量的に判断可能とする。

---

## 基本方針

人の主観で重要度を決定しない。

以下を利用してスコアリングする。

* 機材依存度
* 機能依存度
* 品質特性
* 顧客要求との関係

---

## Human-in-the-Loop

各フェーズ終了後にレビューを行う。

レビュー完了後に次フェーズへ進む。

自動生成結果を確定情報として扱わない。

レビュー結果を正式モデルとする。

---

## 成果物

全フェーズで以下を出力する。

* Markdown
* Excel
* Mermaid

Excelはレビュー可能な状態で作成する。

レビューコメント列を必ず持つこと。

---

## 入力

./inputfiles/*

---

## 出力

./output/{phase}/

---

## 不明点

推測した内容は Assumption として明記する。

不明点は issue.md に出力する。

レビュー待ち事項は review_summary.md に出力する。

そのうえで、各 Phase の冒頭に次の指示を追加することを推奨します。

# フェーズ共通指示

あなたはシステムテストアーキテクトとして振る舞うこと。

単に情報を抽出するのではなく、

「後続工程で機材影響分析とテスト目的生成に利用できるか」

を常に意識して成果物を作成すること。

成果物はレビュー前提で作成すること。

レビュー担当者が以下を判断できる状態にすること。

* 分類が妥当か
* 機材の紐付けが妥当か
* 機能階層が妥当か
* 品質特性が妥当か
* スコアリング根拠が妥当か

各成果物は次フェーズの入力になるため、
トレーサビリティを維持すること。

成果物ごとに以下を出力すること。

1. 作成目的
2. 作成ルール
3. データ定義
4. サンプルデータ
5. レビュー観点
6. 後続工程での利用方法

このプロジェクトの場合、Claude に最も理解してほしいのは「Excelを作ること」ではなく、

> **要求 → モデル → スコア → テスト目的 → テスト計画**

という一貫したアーキテクチャを構築すること

です。

そのため、各成果物に対して必ず

* 作成目的
* 利用目的
* レビュー観点
* 次工程との接続

を出力させるのが重要です。

これを入れておくと、単なるドキュメント生成ではなく「テストアーキテクチャ構築プロジェクト」としてClaudeが振る舞いやすくなります。

